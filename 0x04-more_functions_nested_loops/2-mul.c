#include "main.h"
/**
 * mul- multiplies two digits
 * @a: first digit it recieves
 * @b: second digit it recieves
 * Return: result always
 */
int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
