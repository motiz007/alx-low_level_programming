#include "main.h"
/**
 * add- adds two integers
 * @num1: first number to be entred
 * @num2: second number to be entered
 * Return: sum of the two numbers
 */
int add(int num1, int num2)
{
	int sum;

	sum = num1 + num2;
	return (sum);
}
